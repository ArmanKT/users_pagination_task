import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
static const primary = Color(0xFF6C5DD3);       // Cool violet
static const primaryLight = Color(0xFF9C8FF0);  // Lighter, pastel violet
static const primaryDark = Color(0xFF3F2D91);   // Darker, deep violet

  // Backgrounds
  static const background = Color(0xFFF5F7FA);
  static const surface = Color(0xFFFFFFFF);

  // Text Colors
  static const textPrimary = Color(0xFF1E1E1E);
  static const textSecondary = Color(0xFF6C6C6C);
  static const textOnPrimary = Color(0xFFFFFFFF);

  // Borders and Dividers
  static const border = Color(0xFFE3E6EC);

  // Status Colors
  static const success = Color(0xFF0ABF53);
  static const error = Color(0xFFE53935);
  static const info = Color(0xFF2979FF);
}